@AGENTS.md

# Git Workflow

- Code changes stay local only — do NOT commit or push automatically.
- User says **"push"** → commit and push to `origin/dev/dharmaworker`.
- User says **"merge"** → merge `dev/dharmaworker` into `main` and push to `origin/main`.
